require "spec_helper"

RSpec.describe ObServ::Sidekiq do
  it "has a version number" do
    expect(ObServ::Sidekiq::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
